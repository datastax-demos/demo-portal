HASH=`date +%s.%N | md5sum | cut -f 1 -d ' '`
echo $HASH

cp -r  ~/webapps/demos-web-gui/vagrant/single-node-demo/ launched-clusters/${HASH}
cd launched-clusters/${HASH}
DEMOS_WEB_GUI=~/webapps/demos-web-gui/ vagrant up --provider aws --destroy-on-error
vagrant awsinfo -k public_ip

